# CLAUDE.md

## GitHub account

このフォルダ（`kumamoto`）配下の作業では、GitHub アカウント **`fadysan-rh`** を使用する。
- `gh` CLI操作、`git push`/`git clone`（HTTPS認証）など、GitHubアカウントが関わる操作はすべて `fadysan-rh` を使うこと。
- 他のアカウントが `gh auth status` でデフォルトになっている場合は、このフォルダでの操作前に `gh auth switch -u fadysan-rh` で切り替える。
